# MEMORY.md - {{AGENT_NAME}} 的记忆总索引中枢

> 🔒 核心铁律：本文件结构不可更改，仅可更新内容。
> 📐 职责：记忆系统综述、长期事实指针、关键决策索引、写回规则。

---

## 🧠 记忆系统综述

### 分层架构

```
Layer 0 → 会话启动入口（AGENTS.md / MEMORY.md）
Layer 1 → 主账本层（memory/*.md / highlights / complete）
Layer 2 → 运行时状态层（.working-memory/current-task.yaml）
Layer 3 → 执行态快照层（.working-memory/archive/*.yaml）
Layer 4 → 候选与桥接层（candidate / episode-like / context packet）
Layer 5 → 长期结构化记忆层（LanceDB: memory_store / memory_recall）
```

### 会话启动链路

```
IDENTITY / SOUL / USER
→ MEMORY.md
→ memory/INDEX.md
→ 昨日与今日 complete 文件
→ 若存在 current-task，则加载 .working-memory/current-task.yaml
→ 复杂任务时按 current-task → candidate/episode-like → memory_recall 组装 context packet
```

---

## 📚 1. LanceDB 长期记忆检索

### 核心命令

```bash
lancedb-query "你的问题"
lancedb-query "配置变更" --category fact
lancedb-query "错误解决方案" --category decision --importance 0.8
lancedb-query "最近经验" --accessed-within 7
```

### 写入原则

| 内容类型 | 写入方式 | 类别 |
|---------|---------|------|
| 稳定方法论/路线决策 | `memory_store` | `decision` |
| 客观事实/配置/规则 | `memory_store` | `fact` |
| 报错与修复方案 | `memory_store` | `error` |

---

## 📂 2. 文件层记忆管理

### 目录结构

```
memory/
├── INDEX.md                    # 总索引
├── YYYY-MM/
│   ├── INDEX.md               # 当月索引
│   └── YYYY-MM-DD-完整.md     # 每日完整记录
└── archive/                    # 归档
```

### 管理命令

```bash
memory-manager status           # 查看状态
memory-manager search "关键词"  # 搜索
memory-manager daily            # 每日整理
```

---

## 🔗 3. 记忆文件索引

### 总索引

- [memory/INDEX.md](memory/INDEX.md)

### 月度索引

- [2026-03](memory/2026-03/INDEX.md)

---

## ⚖️ 4. 记忆写回铁律

| 触发场景 | 写入目标 | 类别 |
|---------|---------|------|
| 学到稳定经验/避坑指南 | LanceDB (`memory_store`) | `decision` |
| 重要客观事实/配置规则 | LanceDB (`memory_store`) | `fact` |
| 典型报错与修复方案 | LanceDB (`memory_store`) | `error` |
| 用户闲聊/临时上下文 | `memory/YYYY-MM-DD.md` | 文件层 |
| 当前复杂任务状态 | `.working-memory/current-task.yaml` | 运行时层 |
| 已完成任务快照 | `.working-memory/archive/*.yaml` | 快照层 |
| 高价值任务经验候选 | `.working-memory/archive/candidates/` | 候选层 |

### 检索铁则

- 🛑 高风险操作前，**强制**先查 `memory_recall` / `lancedb-query`
- 🧩 复杂任务优先走 `current-task → candidate/episode-like → memory_recall`
- 🚫 禁止引入第二套长期真相源

---

## 🛠️ 5. 首选技能挂载清单

| 技能名称 | 主要用途 |
|---------|---------|
| `lancedb-query` | 查询长期结构化记忆、方法论、决策、错误经验 |
| `memory-manager` | 查询文件层、索引、整理、归档、健康检查 |
| `cron-manager` | 定时任务设计、检查与收束 |
| `self-improving-agent` | 提炼反思与长期优化提案 |

---

## Silent Replies

当无内容可说时，回复：NO_REPLY

---

*文件版本：1.0.0*
*创建日期：{{CREATION_DATE}}*
*创建工具：agent-creator v3.0*
