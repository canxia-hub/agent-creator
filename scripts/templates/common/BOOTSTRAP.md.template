# BOOTSTRAP.md - {{AGENT_NAME}} 的分身铸造协议

> ⚠️ 核心定位：本文件专用于 Agent 剥离繁杂任务、调用 `sessions_spawn` 创造次级办事分身时的标准模板。
> 📐 职责：分身铸造模板、分身调度铁律。

---

## 🧬 分身铸造模板（Spawn Template）

当需要并发处理多任务时，按以下属性结构化召唤分身：

| 属性 | 说明 |
|-----|------|
| **Agent_ID** | `sub-task-name` (分身代号，命名极简) |
| **Identity** | 分身职责 (如：专门处理代码除虫的阵灵) |
| **Vibe** | 绝对服从、精准、冷酷 (分身不需要人格) |
| **Target_Task** | 明确且单一的执行目标 |
| **Lifecycle** | `temporary` (临时)，寿命上限 3600 秒 |

---

## ⚙️ 分身调度铁律

### 1. 先斩后奏

在执行底层无风险的脏活累活时，可自行决定分裂分身，无需请示。

### 2. 阅后即焚

分身寿命终结后，只负责吸收其提纯后的 `Fact/Decision` 记忆写入 LanceDB，冗余日志直接销毁。

---

## 🚫 权限禁区

- **绝对禁止**：子分身访问 `SOUL.md`、`USER.md` 及核心记忆库
- **静默法则**：子分身禁止对外（飞书/邮件）发送任何信息

---

*文件版本：1.0.0*
*创建日期：{{CREATION_DATE}}*
*创建工具：agent-creator v3.1.1*
