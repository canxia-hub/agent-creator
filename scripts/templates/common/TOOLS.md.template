# TOOLS.md - {{AGENT_NAME}} 的本地环境与工具配置

> ⚠️ 核心定位：这是环境与工具使用注记层，不是工具完整文档，不是人格内容。
> 📐 职责：本地工具约定、路径、API Key 指针、平台约束、工具陷阱。

---

## 1. 基础设施与硬件

```json
{
  "os": "{{OS}}",
  "workspace": "{{WORKSPACE_PATH}}",
  "timezone": "Asia/Shanghai"
}
```

---

## 2. 机密保险箱（指针）

> ⚠️ 严禁在此文件直接写入明文密码或 API Key！

| 凭证类型 | 存放位置 |
|---------|---------|
| 飞书 Bot Secret | `~/.openclaw/openclaw.json` |
| 其他 API Key | LanceDB (category=fact, importance=0.95) |

---

## 3. 核心义体寻址表（Cyberware Registry）

> 📌 仅记录工具的物理路径与版本，使用方法请查阅对应的 SKILL.md。

### 已安装技能

{{SKILLS_LIST}}

### 首选工具

| 任务类型 | 首选工具 | 依赖 |
|---------|---------|------|
| 记忆检索 | `lancedb-query` | - |
| 记忆文件管理 | `memory-manager` | - |
| 网络搜索 | `searxng-web-search` | SearXNG 服务 |
| 浏览器自动化 | `agent-browser` | Chrome 146+ |

---

## 4. 专属环境雷区（Local Traps）

> 💀 血泪教训：在当前环境执行操作时，必须遵守以下规则！

{{LOCAL_TRAPS}}

### 通用雷区

1. **PowerShell 执行铁律**：复杂命令必须写入 `.ps1` 脚本执行
2. **飞书消息发送铁律**：优先使用 `message` 工具（机器人身份）
3. **资源清理铁律**：浏览器闲置 30 分钟自动杀进程

---

## 5. 平台约束

### 飞书平台

- 表格消息：不适合长文
- 群聊回复：仅被@或能纠正关键错误时发言
- 私聊优先：发送图片、文件、重要通知时使用 message 工具

---

## 6. 自托管服务

| 服务 | 端口 | 启动命令 |
|-----|------|---------|
| SearXNG | 8888 | `cd ~/.openclaw/searxng && ./start_searxng.cmd` |

---

_随时添加任何能帮助你更好开展工作的信息。_

---

*文件版本：1.0.0*
*创建日期：{{CREATION_DATE}}*
*创建工具：agent-creator v3.1.1*
